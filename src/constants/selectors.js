const selectors = {
    "longitudeLabel" : ".location__longitude_label",
    "latitudeLabel" : ".location__latitude_label",
    "longitudeValue" : ".location__longitude_value",
    "latitudeValue" : ".location__latitude_value",
    "weatherOvercast": ".weather_overcast",
    "locationButton": ".location_button",
}

Object.freeze(selectors);

export default selectors;